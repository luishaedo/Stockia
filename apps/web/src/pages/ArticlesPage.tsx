import { Card } from '../components/ui/Card';

export function ArticlesPage() {
    return (
        <Card title="Artículos">
            <p className="text-slate-300">Esta sección queda habilitada para navegación, pero por ahora no tendrá cambios funcionales.</p>
        </Card>
    );
}
